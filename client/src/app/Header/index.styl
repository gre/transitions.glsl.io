.app-header {
  background: #000;
  color: #fff;
  min-height: 40px;
  line-height: 40px;
  padding: 0 10px;
  .notsupported {
    display: block;
    background: #aaa;
    margin: 0 -10px;
    text-align: center;
    color: #a00;
    a {
      color: inherit;
      &:hover {
        text-decoration: underline;
      }
    }
  }
  .loader {
    position: absolute;
    left: 10px;
    top: 10px;
    transition: none;
    opacity: 0;
    i {
      display: none;
    }
    &.loading {
      transition: 200ms 200ms;
      opacity: 0.5;
      i {
        display: block;
      }
    }
  }
  h1 {
    margin: 0;
    padding: 0;
    font-size: 1.4em;
    float: left;
    margin-left: 40px;
    margin-right: 40px;
  }
  .github {
    display: inline-block;
    float: right;
    color: #888;
  }
  nav {
    display: inline-block;
    a {
      padding: 0 10px;
      display: inline-block;
      &.current {
        color: #fff;
        background: #444;
        &.about {
          color: #000;
          background: #eee;
        }
      }
    }
  }
}
